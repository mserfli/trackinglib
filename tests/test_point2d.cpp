#include "gtest/gtest.h"
#include <trackingLib/base/point2d.h>

// NOLINTBEGIN(modernize-use-trailing-return-type)

TEST(IntMatrixBasicOpTest, AddingTwoMatrix) 
{
    EXPECT_EQ(1, 1);
}

// NOLINTEND(modernize-use-trailing-return-type)
